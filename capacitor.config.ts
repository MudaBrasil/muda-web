// import { CapacitorConfig } from '@capacitor/cli'

const config = {
  appId: 'com.muda.webapp',
  appName: 'Muda',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
}

export default config
